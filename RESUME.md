# RESUME — Jarvis

**On open:** Read `WORKLOG.md` — pending `[ ]` tasks are the todo, completed `[x]` entries are the history. Derive current status from there.
**During session:** Mark tasks `[x]` with date + conventional commit message immediately when done — not at session end.
**On close:** Verify all completed tasks this session are marked `[x]` in `WORKLOG.md`.

---

## Current Status

Phase 0 — no code yet.

**Next task:** Map OpenJarvis repo structure → update `_index.md` → run `writing-plans` to produce the implementation plan before any code is written.

## Plan File

TBD — `writing-plans` to run before any code starts.
